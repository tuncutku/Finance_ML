import matplotlib.pyplot as plt
import numpy as np

from Neural_Networks import Perceptron


def generate_dataset(n_points, n_features):
    """
    Generate dataset for 1 layer neural network and split it into training and test portions
    
    """


def train_model():
    """
    Train model
    
    Return:
        a tuple of:
        - Model fitted weights, np.array
    """


def plot_results():


if __name__ == "__main__":

    n_points = 100
    n_features = 2

    generate_dataset(n_points,n_features)
